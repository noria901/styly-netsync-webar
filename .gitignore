node_modules/
dist/
*.log

# 8th Wall engine — ships under its own limited-use licence, so it is not
# vendored here. Pulled locally per the README, and by CI at build time.
public/external/xr/
public/external/scripts/
public/external/landing-page/

# Image targets are part of the project — commit them, or the deployed site has
# nothing to recognise. Uncomment to keep them out.
# image-targets/*.json
